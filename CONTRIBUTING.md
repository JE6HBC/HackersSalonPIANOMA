# コントリビューションガイド

Hacker's salon @ PIANOMA は、参加者と一緒に育てるオープンな場です。
このリポジトリには**イベントそのもの**（開催予定・レポート・AI課題）と、
**当日使うシステム**（会場サイネージ・トーナメントタイマー・運営コンソール）の両方が入っています。

初めてのPull Requestでも大歓迎です。チェックリストが埋まらなくても出してください、一緒に直します。

---

## 1. 3つの入口

やりたいことによって、触る場所と必要な知識が変わります。

| やりたいこと                                                       | 触る場所            | 必要なもの                                 |
| :----------------------------------------------------------------- | :------------------ | :----------------------------------------- |
| **コンテンツ** 開催情報・レポート・AI課題・受賞記録を足す/直す     | `data/`             | GitHubアカウントだけ。ブラウザ上の編集でOK |
| **アプリ** サイト・サイネージ・タイマー・運営コンソールを作る/直す | `src/`, `scripts/`  | Node.js 24、TypeScript、Astro              |
| **ルール** 運営フロー・ドキュメント・CIを整える                    | `docs/`, `.github/` | 読んで考える気力                           |

迷ったら Issue を立ててください。[Issueテンプレート](https://github.com/JE6HBC/HackersSalonPIANOMA/issues/new/choose) から選べます。

---

## 2. セットアップ（アプリを触る人向け）

```bash
git clone https://github.com/JE6HBC/HackersSalonPIANOMA.git
cd HackersSalonPIANOMA
npm ci          # Node.js 24（.nvmrc参照）
npm run dev     # http://localhost:4321/HackersSalonPIANOMA/
```

よく使うコマンド:

| コマンド                           | 何をするか                                                |
| :--------------------------------- | :-------------------------------------------------------- |
| `npm run dev`                      | 開発サーバ                                                |
| `npm run verify`                   | **PR前にこれ。** 整形・データ整合性・型・ビルドを全部通す |
| `npm run validate:data`            | `data/` のファイル間参照と個人情報混入をチェック          |
| `npm run format`                   | Prettier で整形                                           |
| `npm run new:event -- 2026-11-22`  | 新しい開催回の雛形を作る                                  |
| `npm run new:challenge -- my-slug` | 新しいAI課題の雛形を作る                                  |

コンテンツだけ触る人は、GitHubのWeb UIで編集してPRを出しても構いません。
CIが同じチェックを回すので、手元で動かせなくても壊れたまま入ることはありません。

---

## 3. ブランチ

`main` に直接pushしないでください。ブランチを切ってPull Requestを出します。

```
<種別>/<短い説明>
```

| 種別         | 使う場面           | 例                                  |
| :----------- | :----------------- | :---------------------------------- |
| `feat/`      | 機能追加           | `feat/tournament-sound-alert`       |
| `fix/`       | 不具合修正         | `fix/timer-drift-on-background-tab` |
| `event/`     | 開催回の追加・更新 | `event/2026-11-22`                  |
| `challenge/` | AI課題の追加       | `challenge/regex-refactor`          |
| `docs/`      | ドキュメント       | `docs/operations-runbook`           |
| `chore/`     | 設定・依存関係     | `chore/bump-astro`                  |

---

## 4. コミットメッセージ

[Conventional Commits](https://www.conventionalcommits.org/ja/) に従います。

```
<type>(<scope>): <日本語でも英語でもOKな要約>
```

- **type**: `feat` / `fix` / `docs` / `style` / `refactor` / `test` / `chore`
- **scope**（任意）: `signage` / `timer` / `admin` / `data` / `challenges` / `ci` など

```
feat(timer): 残り30秒で画面を赤くする
fix(signage): 深夜プログラムが NOW に出ない問題を修正
docs(data): participants.json の項目表を追加
chore(data): 2026-11-22 の開催回を追加
```

**なぜこの形式か:** 後から「いつ何が変わったか」を追いやすくするためです。
形式が崩れていてもPRは落としません。レビューで直します。

---

## 5. Pull Request

### 大きさ

**1つのPRで1つのことだけ。** レビューできる大きさに保ってください。
「ついでにフォーマットも直した」は、差分が読めなくなるので分けてください。

### 出す前に

```bash
npm run verify
```

これが通っていればCIも通ります。落ちた場合、エラーメッセージが直し方まで書いているはずです。

### レビュー

- レビュー担当は [CODEOWNERS](.github/CODEOWNERS) で自動的に割り当てられます
- **コンテンツのPR**（`data/` だけの変更）は、明らかな間違いがなければ基本的に通します
- **アプリのPR**は、動作確認の方法が書かれているかを見ます
- **スキーマ・ルール・CIの変更**は、理由（なぜ今変えるのか）を必ず書いてください
- 返事は数日以内を目安に。急ぎのものはIssueかPRで「当日使います」と書いてください

### マージ

`main` へは **Squash and merge** を使います。履歴を1PR=1コミットに保つためです。

---

## 6. コードを書くときの約束

土台として守ってほしいことだけ書きます。細かいスタイルはPrettierが決めます。

### 全体

1. **データとコードを混ぜない。** イベントの内容・課題・受賞はすべて `data/` に置きます。
   `src/` に日付や人名をハードコードしないでください。
2. **スキーマは `src/content.config.ts` が唯一の正。** ここを通らないデータは存在しないものとして扱います。
   項目を増やすときは、雛形（`data/events/_template/`）と `docs/data-model.md` も同時に更新します。
3. **内部リンクは `href()` を通す。** GitHub Pages は `/HackersSalonPIANOMA/` 配下に配信されるため、
   `href="/events"` と直書きすると本番で404になります（`src/lib/url.ts`）。
4. **外部の依存を気軽に増やさない。** 現状の依存は Astro と Prettier だけです。
   ライブラリを足したいときは、PRに「なぜ自前で書かないのか」を書いてください。

### 見た目

5. **色・余白・文字サイズは `src/styles/tokens.css` から使う。** コンポーネントに生の `#rrggbb` を書かない。
6. **サイネージは「3〜8m離れた暗い部屋から読める」が基準。** 迷ったら文字を大きく、情報を減らす。

### 当日動くもの

7. **会場で壊れたときに、その場で直せる設計にする。** 凝った同期より、リロードで復帰するほうが価値があります。
8. **`localStorage` の読み書きは必ず try/catch で囲む。** プライベートモードで例外が飛びます。

---

## 7. やってはいけないこと

| 禁止                                                                          | 理由                                                                                   |
| :---------------------------------------------------------------------------- | :------------------------------------------------------------------------------------- |
| `data/` に本名・メールアドレス・電話番号・勤務先を書く                        | **このリポジトリは公開です。** 詳細は [docs/policy/privacy.md](docs/policy/privacy.md) |
| APIキー・トークン・パスワードをコミットする                                   | 公開されます。漏れたら即失効させてください                                             |
| 業務上の機密コードをAI課題に貼る                                              | 課題は匿名化・抽象化してから投稿してください                                           |
| 他人の写真を許可なく上げる                                                    | 本人の同意を取ってから                                                                 |
| `main` への直push・force push                                                 | レビューを通さない変更は入れません                                                     |
| 既存のID（`event.id` / `participant.id` / `award.id` / `challenge.id`）の変更 | 他のファイルから参照されています。破壊的変更として扱います                             |

CI（`npm run validate:data`）がメールアドレス・電話番号らしき文字列を検出して落としますが、
最後の砦であって最初の砦ではありません。書く前に気をつけてください。

---

## 8. ライセンス

- **コード**（`src/`, `scripts/`, 設定ファイル）: [MIT License](LICENSE)
- **コンテンツ**（`data/`, `docs/`, `README.md`）: [CC BY 4.0](LICENSE-CONTENT)

Pull Requestを出した時点で、この条件での公開に同意したものとみなします。
AI課題を投稿する場合は、その課題が自分の著作物であるか、CC BY 4.0で再配布できることを確認してください。

---

## 9. 行動規範

参加者・コントリビューター全員に [CODE_OF_CONDUCT.md](CODE_OF_CONDUCT.md) が適用されます。
非エンジニア、初心者、プロを問わず、互いのバックグラウンドを尊重してください。

---

## 10. 困ったとき

| 状況                           | どうするか                                                                        |
| :----------------------------- | :-------------------------------------------------------------------------------- |
| PRの出し方が分からない         | Issueに書いてください。運営が代わりにPRにします                                   |
| CIが落ちて意味が分からない     | PRにそのまま貼ってください。読み解きます                                          |
| 当日、会場でシステムが動かない | [docs/operations-runbook.md](docs/operations-runbook.md) の「困ったときの逃げ道」 |
| 設計の相談をしたい             | [ADR](docs/adr/) を読んでから、提案Issueを立ててください                          |
