{
  "name": "hackers-salon-pianoma",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Hacker's Bar @ Onarimon PIANOMA — イベント運営サイト / 会場サイネージ / AI課題DB",
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "dev": "astro dev",
    "build": "astro build",
    "preview": "astro preview",
    "check": "astro check",
    "validate:data": "node scripts/validate-data.mjs",
    "format": "prettier --write .",
    "format:check": "prettier --check .",
    "new:event": "node scripts/new-event.mjs",
    "new:challenge": "node scripts/new-challenge.mjs",
    "verify": "npm run format:check && npm run validate:data && npm run check && npm run build"
  },
  "dependencies": {
    "astro": "^5.6.1"
  },
  "devDependencies": {
    "@astrojs/check": "^0.9.4",
    "prettier": "^3.5.3",
    "prettier-plugin-astro": "^0.14.1",
    "typescript": "^5.8.3"
  }
}
